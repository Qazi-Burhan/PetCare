# Public, non-sensitive application metadata only.
NEXT_PUBLIC_APP_NAME=PetCare

# No private environment variables are required for the current health check.
# Add server-only variables here only when a private backend integration exists.
